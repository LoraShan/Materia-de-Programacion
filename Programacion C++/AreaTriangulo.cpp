#include <iostream>
using namespace std;

int main() {
    float base, altura;
    cout << "Ingrese base y altura: ";
    cin >> base >> altura;
    cout << "El area del triangulo es: " << (base * altura) / 2 << endl;
    return 0;
}
